# v1.2.0 完整平台交付检查

检查日期：2026-09-07（北京时间）。下面只记录实际证据，不以代码存在替代上线。

## 已完成

- Windows 桌面离线回归 435 项通过。
- 后端完整离线回归 472 项通过；两条测试依赖弃用警告，不影响当前结果。
- PostgreSQL 身份设置单请求合并后，数据库适配器 37 项针对性回归通过。
- 桌面和后端依赖分别 `pip check` 通过，源码 Ruff 检查通过。
- Supabase 现有项目 `rvtjveplqnelgwnpuqtn`，严格 TLS `verify-full` 验证通过。
- 实际创建 `medical_app_platform`，27 张表，列、所属标记和策略集合核对通过。
- 全部 27 张表 `ENABLE/FORCE RLS`；anon/authenticated/service_role 的81个表组合
  无 schema 使用或表数据操作权限。旧4张试点表及迁移历史空间保留。
- Alembic 实际版本 `platform_0001`。没有导入真实历史账号、聊天或健康数据。
- 实际建立专用运行角色 `medical_app_platform_runtime`，连接上限5，逐表最小权限，
  无超级用户、建库、建角色、复制、绕过RLS或角色成员权限。
- 专用运行角色真实登录及数据库就绪验收通过（包括可回滚共享限流写入探针）。
- 真实 PostgreSQL 全业务 43 步回滚验收通过，覆盖三种角色、商城、报告、聊天图片、
  AI 草稿流程和跨账号隔离；未调用收费 AI，合成数据无残留，临时测试授权已恢复。
- 批量身份设置在真实连接验证通过，连接池复用后身份上下文清空验证通过。
- 指定初始账号 `wuyufeng` 已创建为运营管理员；密码仅隐藏输入并哈希存储。
- 源码252个文件已同步现有仓库 `application/`，逐文件 SHA-256 一致；
  首次推送提交 `176a152d3b017b8daa832a4110b40bb13e65a278` 已与远端 main 核对。
- 最终桌面回归再次435项通过，独立只读安全复核未发现新的必修问题。
- 成品验收脚本已补齐云模块存在性、最终 EXE 内嵌归档与源码一致性检查；
  新增31项离线自测通过，文案统一为本地/云端边界。这不代表本次 EXE 已生成。

## 当前部署阻塞（不是已上线）

- Render 工作区 `medical-app`（`tea-dae19ruq1p3s73fivbp0`）已确认；服务列表为空。
- 显式 `plan=free` 创建接口返回 HTTP 402，要求付款信息；未创建服务或付费资源。
- 用户明确确认后，网页以 Free（$0/月）提交，配置 `application` 根目录、Python 3、
  Singapore、后端构建/启动命令、四项非敏感环境变量及 `/health/ready` 健康检查。
- 网页同样弹出 Add Card，提示进行 $1 USD 临时授权验证；已停在此处，没有输入银行卡。
  提交后的服务列表仍为空。必须由用户决定并亲自完成付款信息验证，不能代办或绕过。
- 绝不在源码中填写猜测的公网地址，当前桌面默认地址仍为空；云端能力尚不可公网使用。
- 不操作用户银行卡，不更换付费套餐，也不因部署失败而关闭数据库 TLS 验证。

## 进行中，尚不能视为完成

- Render 免费服务正式部署、公网 HTTPS 双客户端验收、管理员公网登录/退出验收。
- 最终 EXE 便携包构建和验证。

完整配置和边界见 [部署说明](PLATFORM_DEPLOYMENT.md)。
免费服务可能休眠、暂停或超出配额，不宣称“万无一失”或长期生产级可用。
