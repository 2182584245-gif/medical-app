# 1.7.0 本地发布验收（2026-09-19）

范围仅为 Windows 电脑版与源码发布。未部署阿里云新后端，未导入或改写真正的云端资料。

## 实际结果

- 最终桌面全量测试：1,019 通过，0 失败，0 跳过（413.91 秒）。
- 组装说明更新后的打包/本地授权相关测试：78 通过；Ruff 与 Git 差异空白检查通过。
- 后端全量：1,213 通过，37 项环境相关跳过；最后的账号有效期补丁之后，相关 58 项
  全部通过，含 4 项真实临时 PostgreSQL 17 非管理员权限/RLS 测试。
- 开发者离屏 Qt 旅程：6 项通过，检查 20 张界面截图，包含 20/24 像素大字号。
- 已确认默认 DeepSeek Key 在本机 DPAPI 中存在且与要求一致；未发送付费 AI 请求。
- 57 项锁定构建依赖一致，113 项源码/配置冻结不变，最终 EXE 中 106 个应用模块与
  被测试源码逐项匹配。文件版本 1.7.0.0，产品版本 1.7.0。
- 公共目录与 ZIP 严格校验通过：3,777 个文件、972,904,787 字节；ZIP 399,559,040 字节。
- 公共数据库完整性正常，外键问题 0；五个预设账号验证通过，公开库没有开发者授权。
- 隔离启动观察 8 秒，进程保持运行、日志无错误标记；只终止本次测试进程。
  该检查不是原生鼠标/麦克风/摄像头操作验收，也不声称验证了正常关窗退出。
- Microsoft Defender 自定义扫描返回 0，报告未发现威胁；实时防护保持启用，未增加排除项。
- 现有阿里云 `/aliyun/health/ready` 在本机网络下返回 HTTPS 200 且证书验证成功；
  这只证明当时的既有健康接口可达，不代表 1.7.0 后端已上线或全国所有网络都已测试。

## 文件指纹

```text
Windows ZIP SHA256
6c6d4601ffe082c5317b361873065087a4ca661423eb55e6438115da1c898d02

EXE SHA256
d50e5edcf025b5f9d96d377ef7f6d5ff6657f69100d34184009753e2cd960be8

公共预置数据库 SHA256
58f98d0e3c26863db72157075d16df02149bff1668f4c1bb067f268e01166d4c
```

私人安装副本具有另行创建的本机开发者账号，因此其数据库不使用公共种子摘要，
也不得重新上传公共仓库。公共发布包仍只有五个虚构账号，不包含真实管理员口令或哈希。

## 复现入口与限制

构建：`tools/build_windows_release.py --output-root <新建的外部构建目录>`。
本次构建本身成功，但最初生成汇总报告时受到 Windows 默认文本编码影响；工具已改为
显式 UTF-8，随后以 `--verify-existing` 重新验证同一冻结产物，未跳过任何构建/字节码校验。
组装与验证入口：`packaging/assemble_demo_release.py`、`packaging/verify_demo_release.py`、
`packaging/verify_embedded_source.py`；必须使用对应的独立审核图片映射。

EXE 尚未购买代码签名，Authenticode 状态为 NotSigned。即使 Defender 此次扫描无威胁，
其他杀毒软件或 Windows SmartScreen 仍可能显示未知发布者/信誉提示，不能承诺永不误报。
GitHub Actions 运行结论以本次提交对应页面为准，不能将这份本地验收当作远端 CI 已通过。
