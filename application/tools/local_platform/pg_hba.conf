# Unix sockets stay inside this private container for initdb/health checks.
local all all trust
hostnossl all all all reject
hostssl medical_app_local local_bootstrap all scram-sha-256
hostssl medical_app_local medical_app_platform_runtime all scram-sha-256
host all all all reject
