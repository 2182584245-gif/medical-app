# Private bridge only; no plaintext TCP fallback. Bootstrap socket is password protected.
local all all scram-sha-256
hostssl all aliyun_bootstrap 0.0.0.0/0 scram-sha-256
hostssl medical_app_aliyun medical_app_platform_runtime 0.0.0.0/0 scram-sha-256
host all all 0.0.0.0/0 reject
host all all ::/0 reject
