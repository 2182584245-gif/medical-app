# 部署验收记录 — 2026-09-08

## 真实公网部署验收

在用户明确授权部署、临时 SSH 访问和 Supabase 平台资料复制后，已实际完成以下工作。这一节的公网结果来自真实服务，不是下文的本机隔离测试。

- 服务器：Ubuntu 24.04.2，阿里云双核 4 GB，50 GB 云盘；部署目录 `/opt/medical-app`。未修改服务器密码，没有开放数据库直连端口。
- 默认后端：`https://39.106.166.15/aliyun`；备选后端：`https://39.106.166.15/supabase`。两路 `/health/ready` 均在正常 CA 与主机名验证下返回 200，没有关闭 TLS 验证。
- Caddy 已取得生产 Let's Encrypt IP 短期证书；实际所见证书有效期为 2026-09-08 12:17:38 UTC 至 2026-09-15 04:17:37 UTC。证书由 Caddy 自动续期，持久卷保留；本次没有等待完整续证周期，所以不声称已观察过续证事件。
- 业务镜像 `medical-app-aliyun-api:v2`，镜像摘要 `sha256:ea5921e07ec72c93e36466667a058aef54340861b3cc4bb68e8e08b4d9522d6a`。生产源码来自提交 `1a39189a1f427590edbb008a9613ccc004464c24`，镜像内 55 个 Python 文件与冻结源码逐项 SHA-256 一致。后续文档、验收清理工具和打包测试提交不改变该业务镜像。
- 阿里云 PostgreSQL 17 与两路 API 正常运行，平台结构 `platform_0002`，32 张平台表。API 的兼容性健康元数据仍显示 `1.2.0`；本轮桌面发布版本为 `1.4.0`，二者不是相同的版本字段。
- Supabase 使用既有受限 runtime 账号及 `verify-full` 连接；Supabase 管理密码未离开 Windows。两路 API 的运行密钥、会话 pepper 与数据库网络按设计隔离。
- 已复制源库全部 29 张业务表范围内的现有资料：1 个账号、1 个会话、1 条审计，其他业务表为空。先生成认证加密备份，核验空目标、结构与计划，再在事务中导入并另行复核全部计划行存在。复制保留原编号、密码验证哈希和业务内容，Supabase 原数据保留；没有复制在线会话、限流、请求幂等记录、AI Key 或桌面历史。
- 两条真实公网路线各通过 9 组业务检查：注册与登录、匿名拒绝、分页同步、同请求编号重复提交不重复新增、旧版本冲突保护、图片跨客户端上传下载、64 KiB 分块与本机加密缓存校验、跨账号访问拒绝、全部测试会话注销。使用明确标识的合成账号和无个人信息的图片，没有调用真实 AI 或读取管理员业务详情。
- 主机监听与 Docker 端口映射、转发规则联合核验：部署只公开 Caddy 80/443，5432 和两路 8443 没有主机绑定；原有 SSH 22 保留。没有将本机网络环境中失真的 TCP 连通探测当作“公网全部端口扫描通过”。
- 已完成一次真实 `pg_dump` 备份及独立临时库恢复，核验 32 表、RLS 与恢复计数，通过后只移除本次验证库。已启用每日北京时间备份及每小时容量检查。容量检查实测磁盘使用约 13.4%，可用约 40 GiB，无当前容量告警。
- 当前备份是服务器同盘受限副本，不是异地灾备；未设置外部告警接收器。内部证书需要人工到期前轮换；公网证书由 Caddy 管理。此处不承诺单机高可用、无限容量或零故障。

### 已完成的收尾

- 两路各自只清理本次成功验收的 2 个临时会员及固定关联行（各 16 行），先只读生成并审核计划、再按完整 SHA 确认执行。事务核验所有保留行逐字节一致，原运营账号、会话、审计均保留；共享限流状态和序列未修改。清理工具经 35 项测试（含 5 项本机真实 PostgreSQL 事务测试）及独立只读复核，未接入公网 API 或定时器。
- 清理后独立查询 Supabase：账号 1、会话 1、审计 1，其他业务表空；临时会话和幂等日志清零。再次执行阿里云迁移只读状态核验，结果 `all_planned_rows_present`，没有重跑导入。
- 清理后阿里云备份 `20260908T145827Z-6ae1ef4929494139aba69828b94b9009.dump` 的 SHA-256 为 `f5e856315b1f3582d83ef7c43a504d59ed10cb6991b56463a2d826d4cc452106`。2026-09-08 14:58:37 UTC 恢复验证通过：32 表，账号/会话/审计各 1，其他业务表为空。当前共有 2 份通过恢复验证的归档，没有删除任何归档。
- 本次临时 SSH 公钥已经精确撤销，其他授权文件内容保持原来的 272 字节不变，所有者与权限保持不变；随后使用同一密钥、禁止其他身份和密码回退进行连接，实际收到 `Permission denied`。没有修改服务器密码。
- 撤销后又以正常客户端证书验证复查两路公网健康接口。桌面发布包另外通过启动、逐文件/ZIP 校验和 Windows Defender 扫描，详见本机版本目录的发布验收报告；Defender 本次无威胁不等于绝对安全或已具备发布者签名。

## 本机隔离验收（在公网部署前后独立执行）

结论：部署工具的本机隔离验收通过；**本节所述隔离测试本身没有连接真实云数据库，也没有签发 ACME 正式证书**。实际公网结果已单独列于上节。

环境：本机 Docker Desktop 服务端 29.7.2，Linux x86_64，PostgreSQL 17.11（Debian 17.11-1.pgdg12+2），Caddy 2.11.4。Caddy 使用主任务在目标主机核对过的 ECR 镜像摘要；Compose 同时固定目标 PG 镜像摘要。

## 已执行

- `tests/server/test_aliyun_deployment.py`：21 项通过。包括新目录/保留卷拒绝覆盖、四个独立随机密钥、默认 staging、正式切换显式确认、Supabase 运行账号/CA限制、32 表强制 RLS、仅 80/443 公开、数据网络隔离、源码白名单和不删除归档约定。
- 全部新增 Python 文件格式与静态检查通过。
- `docker compose config`（含 maintenance/supabase profiles）通过。
- 白名单 Dockerfile 重新构建成功；依赖均由带 SHA256 的 Linux 锁安装，`pip check` 通过。实际业务 API 的导入、启动和后续测试使用最终构建镜像，没有用完整工作区覆盖应用代码。
- `offline_smoke --confirm-local`：使用随机名称、所有权标签、本机 internal 网络、新空卷生成真实私有 CA 和证书；首次初始化 32 表成功，第二次只核验不重设密码；实际生产 readiness 验证受限运行角色、强制 RLS、共享认证限流成功。
- 非 root、只读根文件系统、删除 Linux capabilities 的 API 容器启动成功；使用私有 CA 验证内部 HTTPS，并实际访问 `/health/ready` 成功。
- 创建了仅本轮测试使用的随机密码虚构会员；实际 `pg_dump` custom 归档与 `pg_restore --exit-on-error --single-transaction` 恢复成功，恢复副本中 1 位合成会员和完整 32 表通过核验。只删除了本轮成功的临时验证库；源库和备份直到实验资源整体清理前均保留。
- Caddy 2.11.4 `validate` 读取生成的 staging 配置与私有信任池成功；未启动 ACME，不联系公网 API。
- 每轮实验结束只清理本轮生成且标签一致的容器、internal 网络和卷；已再次确认没有遗留 `medical-app.offline-review` 容器/卷。

## 隔离测试本身未覆盖

隔离测试不覆盖公网入站、阿里云安全组、真实服务器资源余量、ACME 签发与续证、Supabase 真实运行 URI/CA、真实 AI/天气调用、正式账号、真实资料迁移、systemd 安装和外部告警、异地备份。其中本轮已实际完成的项目见“真实公网部署验收”；完整续证周期、真实 AI/天气新一轮验证、外部告警接收和异地备份不在本轮公网通过结论内。

内部证书仍是 1 年有效、提前 30 天检查提醒，由管理员轮换；不会声称已有自动私有证书轮换。数据库备份默认全部保留，不自动删除未验证或已验证归档，容量告警需要有人接收；同盘副本不能代替异地备份。
